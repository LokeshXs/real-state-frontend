import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: {
      main:"#004eff",
      contrastText: "#FFFFFF",
    },
    secondary: {
      main: "#a7d5ff",
      contrastText: "#353e61",
    },
  },
});


export default theme;
